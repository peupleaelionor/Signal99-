# PWA Requirements

Add:
- manifest.webmanifest
- app icons
- theme color #050505
- standalone mode
- service worker
- offline fallback
- install prompt discreet and dismissible
- Apple web app meta tags

Goal: the website behaves like an installable mobile app.

Native app later:
- Capacitor or Expo after funnel validation.
